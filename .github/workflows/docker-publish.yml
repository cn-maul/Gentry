name: Build & Publish Docker Image

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: read
  # 推送镜像到 GHCR 需要 packages 写权限
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          # 使用仓库 Secret GHCR_TOKEN（具备 write_package 权限的 PAT）登录，
          # GITHUB_TOKEN 默认缺少写入 GHCR 包的权限
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      # 生成镜像标签：默认分支 latest、tag v* → semver、每次构建附带 sha
      - name: Generate image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/cn-maul/gentry
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          # Dockerfile 位于 backend/，但 context 必须是仓库根（COPY 引用了 frontend/ 与 backend/）
          file: backend/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # GHA 缓存加速重复构建
          cache-from: type=gha
          cache-to: type=gha,mode=max